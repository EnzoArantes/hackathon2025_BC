[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend/backend && python manage.py migrate && python manage.py runserver 0.0.0.0:$PORT"

[variables]
DJANGO_SETTINGS_MODULE = "ai_literacy_backend.settings"